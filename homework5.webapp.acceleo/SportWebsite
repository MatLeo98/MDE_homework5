Results for the CREATE TABLE query:
	
	CREATE TABLE Team (id int PRIMARY KEY, name VARCHAR(50), city VARCHAR(50), nation VARCHAR(50));
	
	CREATE TABLE Player (pid VARCHAR(10) PRIMARY KEY, fullname VARCHAR(50), age VARCHAR(50), nationality VARCHAR(50), goals int);
	
Results for the SELECT query:
	
	SELECT  id, name, city, nation FROM Team;
	
	SELECT  pid, fullname, age, nationality, goals FROM Player;
	
Results for the SELECT(with the primary key) query:
	
	SELECT  id, name, city, nation FROM Team WHERE id = ?;
	
	SELECT  pid, fullname, age, nationality, goals FROM Player WHERE pid = ?;
	
